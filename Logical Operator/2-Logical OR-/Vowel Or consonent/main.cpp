#include<stdio.h>
#include<conio.h.>
main()
{
    char ch;
    scanf("%c",&ch);
    if( ch=='a' || ch=='e' || ch=='i' || ch=='o' || ch=='u' || ch=='A' || ch=='E' || ch=='I' || ch=='O' || ch=='U')
    {
        printf("This is a Vower",ch);
    }else{
        printf("This is a consonent",ch);
         }getch();
}
